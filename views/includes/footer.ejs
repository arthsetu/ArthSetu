<footer class="footer">
    <div class="container">
        <div class="row">

            <div class="col-md-4 mb-4">
                <h5>Arth<span style="color: var(--gold)">Setu</span></h5>
                <p>Smart financial management for college students. Track, save and grow.</p>
            </div>

            <div class="col-md-2 mb-4">
                <h5>Quick Links</h5>
                <a href="/">Home</a>
                <a href="/services">Services</a>
                <a href="/about">About</a>
                <a href="/contact">Contact</a>
            </div>

            <div class="col-md-3 mb-4">
                <h5>Features</h5>
                <a href="/expenses">Expense Tracker</a>
                <a href="#">Budget Planner</a>
                <a href="/ai-advisor">AI Advisor</a>
                <a href="/bills">Bill Splitter</a>
            </div>

            <div class="col-md-3 mb-4">
                <h5>Contact</h5>
                <p>📧 arthsetu.finances.co@gmail.com</p>
                <p>📍 India</p>
                <p>arthsetu_finance</p>
            </div>

        </div>

        <div class="footer-bottom">
            <p>© 2025 ArthSetu. Built with ❤️ for college students.</p>
        </div>
    </div>
</footer>

<!-- ✅ Chat Button -->
<button onclick="toggleChat(event)" id="chatBtn">
  💬
</button>

<!-- ✅ Sidebar Chat -->
<div id="chatSidebar">
  <div id="chatHeader">
    <span>AI Assistant</span>
    <button onclick="toggleChat(event)">✖</button>
  </div>

  <iframe src="https://arthsetuchatbot.onrender.com"></iframe>
</div>

<style>
#chatBtn {
    position: fixed;
    bottom: 20px;
    right: 20px;
    background: #1a7a4a;
    color: white;
    border: none;
    border-radius: 50%;
    width: 60px;
    height: 60px;
    font-size: 24px;
    cursor: pointer;
    box-shadow: 0 5px 15px rgba(0,0,0,0.3);
    transition: all 0.3s ease;
}

/* 🔥 When sidebar open */
#chatBtn.active {
    right: auto;
    left: 20px;   /* 👈 LEFT side */
    background: #e74c3c; /* red close button */
}

/* 🔥 Sidebar container */
#chatSidebar {
  position: fixed;
  top: 0;
  right: -400px;
  width: 400px;
  height: 100vh;
  background: white;
  box-shadow: -5px 0 20px rgba(0,0,0,0.2);
  transition: right 0.3s ease;
  z-index: 1000;
  display: flex;
  flex-direction: column;
}

/* When open */
#chatSidebar.active {
  right: 0;
}

/* Header (above iframe) */
#chatHeader {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding: 15px;
  background: #4CAF50;
  color: white;
  font-weight: bold;
  position: relative;
  z-index: 1002;
}

#chatHeader button {
  background: none;
  border: none;
  color: white;
  font-size: 18px;
  cursor: pointer;
}

/* Chat iframe */
#chatSidebar iframe {
  flex: 1;
  width: 100%;
  border: none;
  z-index: 1000;
}
</style>

<script>
function toggleChat(e) {
  if (e) e.stopPropagation();

  const sidebar = document.getElementById("chatSidebar");
  sidebar.classList.toggle("active");
}

/* ✅ Click outside to close */
document.addEventListener("click", function(e) {
  const sidebar = document.getElementById("chatSidebar");
  const button = document.getElementById("chatBtn");

  if (
    sidebar.classList.contains("active") &&
    !sidebar.contains(e.target) &&
    e.target !== button
  ) {
    sidebar.classList.remove("active");
   
  }
});
</script>
